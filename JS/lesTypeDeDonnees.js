/**
 * les differents type de donnees
 * string -->chaine de caractere
 * number --> un nombre entier positif ou negatif ou decimal
 * boolean -->vrai( true ) ou faut( false ) -->utiliser pour les condition et
 * les boucles ne prend que c'est deux valeur rien d'autre
 * null --> ne contient rien du tout
 * undefined -->etat d'une variable non initialiser
 */

//string
    //toute donnees recupere de votre fichier HTML avec JS est une chaine de caracteres
let stringTest = "Ma chaine de caractere est toujour entourer de guillemets";


//number
//un number est deffinit sans guillemets
let numberTest = 24; //ceci est une variable type number
let numberTest2 = "24"; // ceci est une chaine de caracteres
console.log(typeof numberTest, typeof numberTest2);
//un nombre negatif
let numberTest3 = -42;
//pour un nombre a virgule system anglais on utilise un point
let numberTest4 = 24.5;

//boolean
let booleanTest = true;//soit vrai
let booleanTest1 = false;//soit faut


//null
let testNull = null; //uniquement null point!!!

//undefined
let testUndefined = undefined;//aucune initialisation de la variable
// ou que la variable existe pas




